import { OptionCodeSpecsSpecsOption } from "./option-code-specs-specs-option.model";

export interface OptionCodeSpecsSpecs {
    code: string;
    name: string;
    options: OptionCodeSpecsSpecsOption[];
}